<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>QR Code Only</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100vh;
      width: 100vw;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #f2f2f2; /* Light gray background to show contrast */
      font-family: Arial, sans-serif;
    }

    .page {
      width: 794px;
      height: 1040px; /* A4 size */
      position: relative;
      background: white;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    .qr-box {
      position: absolute;
      bottom: 20px;
      right: 20px;
      text-align: center;
    }

    .qr-box img {
      width: 120px;
      height: 120px;
      border: 1px solid #ccc;
    }

    .qr-box p {
      margin-top: 6px;
      font-size: 12px;
      color: #444;
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="qr-box">
     
      <p>Scan to verify</p>
    </div>
  </div>
</body>
</html>
